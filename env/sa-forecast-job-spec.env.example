# GENERATED by ./bin/prompt build -- do not edit.
# Edit the `vars:` block in prompts/sa-forecast-job-spec.md instead.
#
# Copy to private/env/sa-forecast-job-spec.env (./bin/prompt init does this) and fill in.

# Territory or comma-separated territories this book covers.
#   [REQUIRED]
TERRITORIES=Northern Europe

# Name of the team lead who owns this job.
#   [REQUIRED; private -- real value is NOT in this repo]
SA_TEAM_LEAD=

# Comma-separated architect names whose book this covers.
#   [REQUIRED; private -- real value is NOT in this repo]
SA_TEAM=

# One-line fiscal-year definition, e.g. "FY2030 = 2029-02-01 to 2030-01-31". Drives relative date literals; never hardcode a year in a query.
#   [REQUIRED; private -- real value is NOT in this repo]
FISCAL_CALENDAR=

# The CRM clauses that scope the book to this team and territory. Internal schema.
#   [REQUIRED; private -- real value is NOT in this repo; multiline -- use KEY=@path/to/file.md]
# SCOPE_FILTERS=@../../env/fragments/sa-forecast-scope-filters.example.md   <- shape shown here; substitute your own
SCOPE_FILTERS=

# Sections 2 through 6 -- field map, primary query, completeness score, signal tiers, write constraint. The largest and most organisation-specific fragment.
#   [REQUIRED; private -- real value is NOT in this repo; multiline -- use KEY=@path/to/file.md]
# QUERY_AND_SIGNAL_RULES=@../../env/fragments/sa-forecast-query-and-rules.example.md   <- shape shown here; substitute your own
QUERY_AND_SIGNAL_RULES=

# Link-construction patterns for record references. Contains the CRM org host, which identifies the instance.
#   [REQUIRED; private -- real value is NOT in this repo; multiline -- use KEY=@path/to/file.md]
# LINK_TEMPLATE=@../../env/fragments/sa-forecast-link-template.example.md   <- shape shown here; substitute your own
LINK_TEMPLATE=

# Optional. Unresolved questions about CRM schema, permissions and integrations. Leave unset and section 11 renders empty.
#   [private -- real value is NOT in this repo; multiline -- use KEY=@path/to/file.md]
# OPEN_ITEMS=@../../env/fragments/sa-forecast-open-items.example.md   <- shape shown here; substitute your own
OPEN_ITEMS=
