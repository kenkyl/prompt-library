# GENERATED by ./bin/prompt build -- do not edit.
# Edit the `vars:` block in prompts/sa-forecast-brief.md instead.
#
# Copy to private/env/sa-forecast-brief.env (./bin/prompt init does this) and fill in.

# Default territory or comma-separated list to scope the brief to. Must match the territory values in the CRM exactly. Baked in at build time, NOT taken from an argument -- a scheduled task passes none, so a runtime placeholder would arrive empty. An explicit territory in the invocation overrides it; see the first line of the body.
#   [REQUIRED; private -- real value is NOT in this repo]
TERRITORIES=

# Comma-separated architect names whose book this brief covers.
#   [REQUIRED; private -- real value is NOT in this repo]
SA_TEAM=

# The CRM read tool to load, named as the assistant will see it.
#   [REQUIRED; private -- real value is NOT in this repo]
CRM_TOOL=

# Bullet list of the evidence sources to search in step 3, one per line, each naming the connector and what to look for in it. Local connector configuration, so it lives in the private overlay.
#   [REQUIRED; private -- real value is NOT in this repo; multiline -- use KEY=@path/to/file.md]
# The committed example shows the expected shape. Point at your own file,
# or use the example by replacing the blank line below with:
#   EVIDENCE_SOURCES=@../../env/fragments/sa-forecast-evidence-sources.example.md
EVIDENCE_SOURCES=

# Absolute consumption change below which a row is suppressed from the rollup. Include the currency symbol. Private because a committed default would put a currency figure in a public repo.
#   [REQUIRED; private -- real value is NOT in this repo]
MIN_CONSUMPTION_DELTA=
