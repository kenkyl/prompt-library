# GENERATED by ./bin/prompt build -- do not edit.
# Edit the `vars:` block in prompts/customer-account-intelligence.md instead.
#
# Copy to private/env/customer-account-intelligence.env (./bin/prompt init does this) and fill in.

# Your employer -- the vendor you sell and architect for.
#   [REQUIRED]
COMPANY=Contoso Data Systems

# The customer account this Project is dedicated to. Spell it as the CRM does.
#   [REQUIRED; supplied at invocation as $account in the skill surface]
CUSTOMER_NAME=Northwind Airlines

# Product and methodology vocabulary likely to appear in your meeting notes. Used to drive note searches, so favour recall over precision.
#   [REQUIRED]
PRODUCT_TERMS=cache, cluster, latency, vector, replication, failover, POC, renewal, migration, sizing

# Your company's sales methodology -- stages, gates, qualification framework, artifact chain, and role boundaries. Internal IP; the real value lives in the private overlay, never in this repo.
#   [REQUIRED; private -- real value is NOT in this repo; multiline -- use KEY=@path/to/file.md]
# The committed example shows the expected shape. Point at your own file,
# or use the example by replacing the blank line below with:
#   SALES_METHODOLOGY=@../../env/fragments/sales-methodology.example.md
SALES_METHODOLOGY=

# Optional. Company-specific pricing, rate card, packaging tiers, discount authority, or professional-services thresholds. Leave it unset and the entire pricing section disappears from the rendered prompt. Never commit real figures -- keep them in private/ and point at the file with PRICING_REFERENCE=@../fragments/pricing-reference.md
#   [private -- real value is NOT in this repo; multiline -- use KEY=@path/to/file.md]
# The committed example shows the expected shape. Point at your own file,
# or use the example by replacing the blank line below with:
#   PRICING_REFERENCE=@../../env/fragments/pricing-reference.example.md
PRICING_REFERENCE=

# Markdown bullet list of the recurring deliverables named by your methodology. Internal vocabulary; kept in the private overlay.
#   [REQUIRED; private -- real value is NOT in this repo; multiline -- use KEY=@path/to/file.md]
# The committed example shows the expected shape. Point at your own file,
# or use the example by replacing the blank line below with:
#   METHODOLOGY_DELIVERABLES=@../../env/fragments/methodology-deliverables.example.md
METHODOLOGY_DELIVERABLES=
